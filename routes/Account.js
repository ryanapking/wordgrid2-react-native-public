import React, { Component } from 'react';
import { View, Text, Button } from 'react-native';

export default class Account extends Component {
  render() {
    return (
      <Text>Account Route</Text>
    );
  }
}